language: node_js
node_js:
  - "6.9"
  - "7.1"
sudo: false
cache:
  directories:
    - node_modules
before_install:
script:
  # Run test script, depending on istanbul install
  - "npm test"
after_script: